{
  "name": "student-portal",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "client:build": "npm --prefix client run build",
    "client:dev": "npm --prefix client run dev",
    "client:lint": "npm --prefix client run lint",
    "server:dev": "npm --prefix server run dev",
    "db:migrate": "npm --prefix server run migrate",
    "db:migrate:status": "npm --prefix server run migrate:status",
    "server:start": "npm --prefix server run start",
    "server:test": "npm --prefix server run test",
    "seed:users": "npm --prefix server run seed:users",
    "e2e": "node e2e/browser-smoke.test.mjs",
    "check": "npm run server:test && npm run client:lint && npm run client:build && npm run e2e"
  }
}
